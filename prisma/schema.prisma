datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Park {
  id          Int      @id @default(autoincrement())
  name        String
  description String?
  location    Unsupported("geometry(Point, 4326)")
}
