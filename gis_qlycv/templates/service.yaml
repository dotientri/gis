apiVersion: v1
kind: Service
metadata:
  name: {{ include "gis_qlycv.fullname" . }}
  labels:
    {{- include "gis_qlycv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gis_qlycv.selectorLabels" . | nindent 4 }}
