apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gis_qlycv.fullname" . }}-test-connection"
  labels:
    {{- include "gis_qlycv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gis_qlycv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
